{
  "installation_notes": "In each directory symlinked from node_modules, we need to add a package.json specifying transforms. This is because by default browserify does not apply transforms to symlinked dirs. See https://github.com/babel/babelify/issues/53#issuecomment-76497982 ",
  "babel": {
    "presets": [
      "es2015",
      "react"
    ]
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
